services:
  - type: web
    name: fraud-detection-analytics
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn src.dashboards.server:app
    envVars:
      - key: PORT
        value: 10000
      - key: PYTHON_VERSION
        value: 3.10.0
